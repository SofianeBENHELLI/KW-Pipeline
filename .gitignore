__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv*/
venv/
node_modules/
# Secrets — every variant of `.env` except the committed template.
# `.env.example` is force-included via the `!` rule so contributors
# can still see the schema; everything else is ignored by default.
.env
.env.*
!.env.example
*.pem
*.key
secrets/
.DS_Store
*.egg-info/
.kw-pipeline/
.coverage
.coverage.*
coverage.xml
htmlcov/
pytest-junit.xml
