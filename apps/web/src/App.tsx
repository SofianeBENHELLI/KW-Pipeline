export default function App(): JSX.Element {
  return (
    <main>
      <h1>Orbital — KW Pipeline</h1>
      <p>
        Reviewer surface scaffold. Document ingestion, semantic preview, and
        validate/reject controls land in follow-up issues.
      </p>
    </main>
  );
}
